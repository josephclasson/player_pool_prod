/**
 * Minimum season points per game for pool/draft eligibility and ESPN population.
 * Players below this (or blank PPG) are not written from the ESPN load and are excluded from draft/player pool APIs.
 */
export const MIN_POOL_SEASON_PPG = 4;

/** NCAA tournament field size for committee overall seeds 1–68. */
export const NCAA_BRACKET_TEAM_COUNT = 68;
